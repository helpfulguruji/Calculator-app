name: all_in_one_calculator
description: A privacy-first all-in-one calculator and utility toolkit for everyday calculations.
publish_to: 'none'
version: 1.5.1+7
environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: '>=3.47.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  flutter_lints: ^6.0.0
  google_mobile_ads: ^9.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/brand_assets/
  uses-material-design: true
